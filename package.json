{
  "name": "serverless-api-lambda-trial",
  "version": "1.0.0",
  "description": "Try Serverless framework",
  "main": "index.js",
  "scripts": {
    "start": "sls offline start",
    "deploy": "sls deploy --stage dev --aws-profile sls_user",
    "deploy:prod": "sls deploy --stage prod --aws-profile sls_user",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tayutaedomo <tayutaedomo@gmail.com>",
  "devDependencies": {
    "serverless": "^1.53.0",
    "serverless-offline": "^5.11.0"
  },
  "dependencies": {
    "moment": "^2.29.2"
  }
}
